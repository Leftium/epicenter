{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-overlay-capability",
  "description": "Capability for the floating recording overlay window. The overlay only emits actions and the ready handshake, and listens for status updates; the main window owns its creation, positioning, and visibility.",
  "windows": [
    "recording-overlay"
  ],
  "permissions": [
    "core:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "log:default"
  ]
}
