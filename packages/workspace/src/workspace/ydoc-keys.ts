/**
 * Y.Doc Array Key Conventions — re-exported from `@epicenter/document`.
 *
 * The canonical definitions live in `@epicenter/document` so the document
 * and workspace packages cannot drift. This module re-exports them for
 * backwards compatibility with existing workspace consumers.
 */
export { KV_KEY, TableKey, type KvKey } from '@epicenter/document';
