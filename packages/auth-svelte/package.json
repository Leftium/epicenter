{
	"name": "@epicenter/auth-svelte",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Svelte 5 reactive wrapper around @epicenter/auth. Thin projection that exposes the core's imperative subscriptions as reactive getters.",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "svelte-check --tsconfig ./tsconfig.json"
	},
	"dependencies": {
		"@epicenter/auth": "workspace:*"
	},
	"peerDependencies": {
		"svelte": "^5.0.0"
	},
	"devDependencies": {
		"@types/bun": "catalog:",
		"svelte": "catalog:",
		"svelte-check": "catalog:",
		"typescript": "catalog:"
	},
	"license": "MIT"
}
